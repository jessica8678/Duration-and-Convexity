# Duration & Convexity — 产品设计说明 v2

**状态** 对齐当前实现（duration-convexity.html，三 tab 版）
**交付物** 本文档 + `duration-convexity.html`（单文件、零依赖、双击可跑）
**产品语言** 英文界面；本文档为开发用中文
**观众** 无固定收益背景的 junior 为主，同时经得起量化背景评审者检验

---

## 0. 一句话现状

一个三 tab 的交互看板：**Price and yield → Duration → Convexity**。一根滑块贯穿全站，图形元素随 tab 递进解锁（曲线 → +切线 → +抛物线），公式的每一项含义直接标注在该项正下方，所有术语在数字算出来之后才被命名，主层零微积分词汇。

---

## 1. 核心设计不变量（改任何东西之前先读这节）

这五条是产品的承重墙，历轮迭代中被反复确认，违反任何一条都是回退：

1. **名字随物出场，但算式先到。** 术语可在概念第一次出现时就命名（Macaulay duration 直接做 Step 1 标题），但产生它的加权平均算式必须同屏第一眼可见。禁止的是"名字先于理解"，不是"名字先于段落"。
2. **主层只放行 "Taylor expansion" 一个微积分词。** 仍不出现求导／导数／一阶导／二阶导／微分。替代语言全站统一：Taylor 的两项一律叫 **term 1 / term 2**（图例、blotter、图上标注同步）；÷1.025 是"比例而非绝对值"；convexity 是"加速度 a"；Δy² 是"一次攒速度，一次带着走"。放行 Taylor 的理由：½at² 类比能立刻接住它——junior 见到 "Taylor expansion" 会怕，见到"就是高中物理那个 ½at²"就不怕。
3. **公式注释长在公式身上。** 每一项的含义写在该项正下方（`.aeq` 组件），不用旁置图例、不用散文段落解释公式。
4. **看板上不放解释性散文，只放代入数字后的算式。** 想深挖的路径是提问，不是阅读。每段 `.note` 不超过三句。
5. **语义配色全站唯一**：墨黑=真实价格，蓝 #1B54D6=一阶/duration，紫 #6B3FA0=二阶/convexity，灰=残差。颜色一旦绑定含义不得复用。

---

## 2. 形态决策记录

| 决策 | 内容 | 原因 |
|---|---|---|
| D1 | 双栏叙事（sticky canvas）→ **三 tab** | 用户反馈：单屏同时出现 duration 和 convexity 会吓跑受众；tab 让复杂度由用户自己逐步解锁 |
| D2 | 图形随 tab 累加：Tab1 只有黑曲线，Tab2 出现蓝线，Tab3 出现紫线 | 同 D1，递进披露 |
| D3 | 一根滑块三种读法：Tab1 显示 Yield，Tab2/3 显示 Shock（±bp） | 场景全程唯一，但每个 tab 只暴露当前概念相关的量 |
| D4 | 定价公式只写三项（首笔/次笔/末笔），分数形式，含义在正下方 | 用户反馈两轮：求和号和图例式注释都太复杂 |
| D5 | Duration 拆两步：Step1 平均等待时间（柱图+天平+表格），Step2 一次除法变成敏感度 | 用户反馈：Macaulay 和 Modified 同时出场深度下得太快 |
| D6 | 表格 ↔ 图形联动：hover 表格行高亮对应现金流柱 | 用户要求表格与图形结合 |
| D7 | ~~漂移的斜率框架~~ → **½at² 逐项对照**：t↔Δy、v↔斜率 −779.46、a↔弯曲 7362.87、位移↔价格变化。三角形图 = 速度–时间图，底 = 移动、高 = a×移动、**面积 = 缺口本身** | 用户要求回到第一性原理。漂移框架本质是 ½at² 的口语版，并存即重复。一张三角形同时回答"为什么有 ½"（速度从零匀速涨，只吃到一半 → 三角非矩形）和"为什么平方"（一次攒速度，一次带着走） |
| D8 | ~~斜率漂移三张实测卡片~~ → 已过期。Tab3 只保留图上无数字的 "speed here" 刻度 | 卡片显示的实测斜率与 D10 引入的算术推导链路重复，且是 §5"0.70 vs 0.74"混淆的根源，删除以消歧 |
| D9 | ½at² 物理类比作为 12px 小字旁注 | 对记得高中物理的人一击即中，对其他人无害 |
| D10 | ÷1.025 由**算术推导**取代断言：5% 时每步除 1.0250，6% 时除 1.0300，绝对 +0.0050，但复利只认比例 → 0.0050÷1.0250 = 0.4878%/步；15.9789 步 × 0.4878% = 7.7946% | 原文"抹掉一轮缩水"靠读者信任。枢纽数字 2×D_mac = 15.9789 步必须与 7.9894 同屏出现，否则 Step 2/3 断链 |

---

## 3. 逐 tab 规格（步骤骨架）

> §3 只负责描述**形状**。若复述实现细节必然与 HTML 失配；设计约束由 §1 承担，数值正确性由 §4 承担。

常驻：blotter / 滑块 / 主图 / 页尾 What this leaves out。

**Tab 1** 定价式 → 三项注释 → 实时卡片

**Tab 2**
1. 平均等待
2. 时间即杠杆
3. 比例而非绝对
4. 那就是斜率

**Tab 3**
1. 缺失的 term 2
2. ½at² 逐项对照
3. 代入数字
4. 73.6287 从哪来

---

## 4. 数学基准（实现必须匹配到 4 位小数）

参考债券：面值 100，票息 5% 半年付，10 年，y₀=5%。

```
P₀ = 100.0000   D_mac = 7.9894   D_mod = 7.7946   C = 73.6287
连续复利校验：D_mac = D_mod = 7.983580（分母消失）
解析 vs 中心差分：D_mod 7.794581/7.794581；C 73.6287/73.6252
```

| Δy | P₁ | 实际 ΔP | duration 项 | convexity 项 | 残差 |
|---:|---:|---:|---:|---:|---:|
| +50bp | 96.1932 | −3.8068 | −3.8973 | +0.0920 | −0.0016 |
| +100bp | 92.5613 | −7.4387 | −7.7946 | +0.3681 | −0.0123 |
| −100bp | 108.1757 | +8.1757 | +7.7946 | +0.3681 | +0.0130 |
| +300bp | 79.6145 | −20.3855 | −23.3837 | +3.3133 | −0.3150 |

斜率漂移基准（Tab3 Step2 卡片显示值，单位：元 / 1% 收益率）：

| y | 3% | 4% | 5% | 6% | 7% | 8% |
|---|---:|---:|---:|---:|---:|---:|
| slope | 9.43 | 8.57 | 7.79 | 7.09 | 6.46 | 5.89 |
| drift | +1.64 | +0.78 | 0 | −0.70 | −1.33 | −1.90 |

不对称性：±100bp 下 +8.1757 vs −7.4387，差 0.7370 ≈ 2×凸性项 0.7363。

**回归校验（任何改动后重跑）：**

- 恒等式必须精确成立：2 × 7.9894 = 15.9789 步；0.0050 ÷ 1.0250 = 0.4878%；15.9789 × 0.4878% = 7.7946% = 7.9894 ÷ 1.025。
- 物理映射：v = −D_mod×P = −779.46，a = C×P = 7362.87，½·a·Δy² 在 Δy=1% 时 = +0.3681 = 三角形面积 = 图上紫色缺口。

---

## 5. 已知问题与坑（演示前必读）

1. **hover 联动在触屏上不工作**。手机演示时表格-柱图联动失效（不崩溃，只是无高亮）。
2. **数字精度固定 4 位小数**，滑块步长 10bp 下没问题；如果未来把步长调细到 1bp，小数位需要自适应。
3. 滑块范围 2%–8%；超范围的极端场景（如 12% 高收益）需要改 YLO/YHI 并检查坐标轴。
4. 单文件内嵌全部 JS/CSS，无构建步骤；改动后直接刷新浏览器即可。
5. **首屏是烘焙的静态 SVG**（滑块默认 6% 时的状态，直接写死在 HTML 里）。目的是让不执行脚本的预览环境也能看到图。代价：**改动任何图表逻辑或默认参数后，必须重新烘焙首屏**（用 python 重算坐标替换 `<svg id="cv">` 与 `<svg id="wb">` 的内容），否则无脚本预览会显示与交互不一致的旧图。
6. **聊天/邮件的内嵌预览是静态的**：图能看见（因为第 5 条），但滑块、tab 切换、hover 联动全部不可用——这不是 bug，是预览环境不执行 JavaScript。页面内置 `#jswarn` 黄色横幅，脚本一运行就自动移除：**横幅还在 = 你在预览环境；横幅消失了交互还失灵 = 真 bug**。正确打开方式：下载文件 → 浏览器双击。

### 已解决

- **0.70 vs 0.74 不一致**：曾经的 Step2 卡片显示"拖到当前位置的累计实际漂移"（+100bp 时 −0.70），Step3 公式用"出发点的漂移速度"（73.63 → 每 1% 漂 0.7363），两数接近不相等、容易被追问。D8 已删除显示实测斜率的三张卡片，页面上不再有两个近似但不等的数字并排，混淆源头消失。

---

## 6. 被搁置的想法（v1 规格遗产，按价值排序）

这些在早期规格中设计过、当前实现**没有**的东西。恢复任何一项前先对照第 1 节不变量：

1. **缩放镜头（英雄交互）**：连续放大到 ±1bp 看曲线与切线重合——"局部是直的"的亲眼验证。教学价值最高，实现成本中等。
2. **验证 waterfall**：P₀ → duration → convexity → residual → P₁ 的瀑布图收口（原始需求第 5 点，当前只有三张卡片的弱化版）。
3. **四选一猜价格**（90/92.56/95/99）：利用"用 10 年代替 7.99 年"这个典型错误制造学习需求。
4. **数值验算面板**：解析导数 vs 有限差分并排——面向评审者的最强可信度信号，约十行代码。
5. 预设场景切换（零息/30 年/高收益/可赎回负凸性）、duration 家族对照面板（Macaulay/Modified/Money/DV01）、凸性应用卡片（压力测试/对冲漂移/负凸性）、每 tab 检查题。
6. 已删除且**不要恢复**：翻四倍表（被"移动进来两次"取代）、求和号版公式、图例式注释。

---

## 7. 验收清单（当前形态）

- [ ] 三 tab 切换后图形层正确增减（1 层/2 层/3 层）
- [ ] 滑块拖动时：blotter、图上三角形/缺口条/slope 刻度、所有实时算式与卡片同步跳动
- [ ] 滑到 5.00%（Δy=0）：无 NaN，缺口卡显示 +0.0000，slope 刻度与蓝线重合
- [ ] Tab2 表格 hover 高亮对应柱；表尾加总 7.9894 / 100.00%
- [ ] Tab3 三张斜率卡在 6% 处显示 7.09 / −0.70（对照第 4 节基准表）
- [ ] 键盘左右方向键可切 tab；reduced-motion 无动画
- [ ] 375px 视口下公式区横向可滚、其余不溢出
- [ ] 浏览器打开后 `#jswarn` 横幅自动消失；无脚本环境下横幅可见且首屏两张图完整显示（6% 状态）
